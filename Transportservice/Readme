Transportservice

This service is built using Node.js. It needs a connection to MongoDB and a broker to work properly.

MongoDB:
Database: TransportDB
Collection: tsdatalist

Broker:
Subscription to workflow/claim/#

Currently, the service can be accessed with localhost:3002.

After downloading these files, you will need to do a npm install to download all necessary node_modules. 
